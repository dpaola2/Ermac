var program = {
    name : "Dave",
    x : 5,
    doSomething : function() {
        if (x < 1) {
            return "x is less than 1"; 
        } else if (name == "Dave") {
            return "my name is Dave!";
        }
    }
}